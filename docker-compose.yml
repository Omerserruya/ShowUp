services:

  nginx:
    image: nginx:alpine
    ports:
      - ${HTTP_PORT}:80  # HTTP port for local development
    environment:
      - CORE_SERVICE_URL=${BACKEND_PROTOCOL}://${CORE_SERVICE_HOST}:${CORE_SERVICE_PORT}
      - AUB_SERVICE_URL=${BACKEND_PROTOCOL}://${AUB_SERVICE_HOST}:${AUB_SERVICE_PORT}/
      - DOLLAR=$ # To escape $ in envsubst
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf.template
    command: /bin/sh -c "envsubst < /etc/nginx/conf.d/default.conf.template > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"
    depends_on:
      - aub-service
      - core-service

  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - ${DB_PORT}:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - ${RABBITMQ_PORT}:5672
      - ${RABBITMQ_MANAGEMENT_PORT}:15672
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_DEFAULT_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_DEFAULT_PASS}

  redis:
    image: redis:alpine
    ports:
      - ${REDIS_PORT}:6379
    volumes:
      - redis_data:/data
  
  core-service:
    build:
      context: .
      dockerfile: core-service/Dockerfile
    ports:
      - ${CORE_SERVICE_PORT}:${CORE_SERVICE_PORT}
    environment:
      - PORT=${CORE_SERVICE_PORT}
      - JWT_SECRET=${JWT_SECRET}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=${RABBITMQ_PORT}
      - RABBITMQ_USER=${RABBITMQ_DEFAULT_USER}
      - RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}

    depends_on:
      - postgres
      - rabbitmq

  # aub-service:
  aub-service:
    build:
      context: ./aub-service
      dockerfile: Dockerfile
    ports:
      - ${AUB_SERVICE_PORT}:${AUB_SERVICE_PORT}
    environment:
      - PORT=${AUB_SERVICE_PORT}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=${RABBITMQ_PORT}
      - RABBITMQ_USER=${RABBITMQ_DEFAULT_USER}
      - RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}
      - OUTPOST_QUEUE_NAME=${OUTPOST_QUEUE_NAME}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXP_SECONDS=${JWT_EXP_SECONDS}
      - OTP_TTL_SEC=${OTP_TTL_SEC}
      - OTP_QUEUE_NAME=${OTP_QUEUE_NAME}

    depends_on:
      - postgres
      - rabbitmq
      - redis

  # outpost-service:
  #   build:
  #     context: ./outpost-service
  #     dockerfile: Dockerfile
  #   environment:
  #     - QUEUE_HOST=rabbitmq
  #     - QUEUE_PORT=${RABBITMQ_PORT}
  #     - QUEUE_USER=${RABBITMQ_DEFAULT_USER}
  #     - QUEUE_PASSWORD=${RABBITMQ_DEFAULT_PASS}
  #     - QUEUE_NAME=${OUTPOST_QUEUE_NAME}
  #     - QUEUE_PREFETCH=${OUTPOST_QUEUE_PREFETCH}
  #     - HTTP_PORT=${OUTPOST_HTTP_PORT}
  #     - WHATSAPP_PHONE_NUMBER_ID=${WHATSAPP_PHONE_NUMBER_ID}
  #     - WHATSAPP_ACCESS_TOKEN=${WHATSAPP_ACCESS_TOKEN}
  #     - WHATSAPP_API_BASE=${WHATSAPP_API_BASE}
  #   depends_on:
  #     - rabbitmq


  # schedule-service:
  #   build:
  #     context: ./schedule-service
  #     dockerfile: Dockerfile
  #   ports:
  #     - ${SCHEDULE_SERVICE_PORT}:5004
  #   environment:
  #     - DB_HOST=postgres
  #     - DB_PORT=${DB_PORT}
  #     - DB_USER=${DB_USER}
  #     - DB_PASSWORD=${DB_PASSWORD}
  #     - DB_NAME=${DB_NAME}
  #     - RABBITMQ_HOST=rabbitmq
  #     - RABBITMQ_PORT=${RABBITMQ_PORT}
  #     - RABBITMQ_USER=${RABBITMQ_DEFAULT_USER}
  #     - RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}
  #   depends_on:
  #     - postgres
  #     - rabbitmq
  #     - core-service
  #     - aub-service
  #     - campaign-service
  #     - outpost-service
      
  # campaign-worker:
  #   build:
  #     context: ./campaign-worker
  #     dockerfile: Dockerfile
  #   ports:
  #     - ${CAMPAIGN_SERVICE_PORT}:5002
  #   environment:
  #     - DB_HOST=postgres
  #     - DB_PORT=${DB_PORT}
  #     - DB_USER=${DB_USER}
  #     - DB_PASSWORD=${DB_PASSWORD}
  #     - DB_NAME=${DB_NAME}
  #     - RABBITMQ_HOST=rabbitmq
  #     - RABBITMQ_PORT=${RABBITMQ_PORT}
  #     - RABBITMQ_USER=${RABBITMQ_DEFAULT_USER}
  #     - RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}
  #   depends_on:
  #     - postgres
  #     - rabbitmq
  #     - core-service
  #     - aub-service

  # webhook-hendler:
  #   build:
  #     context: ./webhook-handler
  #     dockerfile: Dockerfile
  #   ports:
  #     - ${WEBHOOK_HANDLER_SERVICE_PORT}:5005
  #   environment:
  #     - DB_HOST=postgres
  #     - DB_PORT=${DB_PORT}
  #     - DB_USER=${DB_USER}
  #     - DB_PASSWORD=${DB_PASSWORD}
  #     - DB_NAME=${DB_NAME}
  #     - RABBITMQ_HOST=rabbitmq
  #     - RABBITMQ_PORT=${RABBITMQ_PORT}
  #     - RABBITMQ_USER=${RABBITMQ_DEFAULT_USER}
  #     - RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}
  #   depends_on:
  #     - postgres
  #     - rabbitmq
  #     - core-service
  #     - aub-service
  #     - campaign-service
  #     - outpost-service
  #     - schedule-service
    
  # webhook-worker:
  #   build:
  #     context: ./webhook-worker
  #     dockerfile: Dockerfile
  #   ports:
  #     - ${WEBHOOK_WORKER_SERVICE_PORT}:5006
  #   environment:
  #     - DB_HOST=postgres
  #     - DB_PORT=${DB_PORT}
  #     - DB_USER=${DB_USER}
  #     - DB_PASSWORD=${DB_PASSWORD}
  #     - DB_NAME=${DB_NAME}
  #     - RABBITMQ_HOST=rabbitmq
  #     - RABBITMQ_PORT=${RABBITMQ_PORT}
  #     - RABBITMQ_USER=${RABBITMQ_DEFAULT_USER}
  #     - RABBITMQ_PASSWORD=${RABBITMQ_DEFAULT_PASS}
  #   depends_on:
  #     - postgres
  #     - rabbitmq
  #     - core-service
  #     - aub-service
  #     - campaign-service
  #     - outpost-service
  #     - schedule-service

networks:
  default:
    driver: bridge
volumes:
  postgres_data:
  redis_data:
